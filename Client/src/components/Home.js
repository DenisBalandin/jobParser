import React, {Component} from 'react';
import { db } from "../firebase";

class Home extends Component{

    render(){
        return(
            <div>
              HOME
            </div>
        );
    }
}
export default Home;